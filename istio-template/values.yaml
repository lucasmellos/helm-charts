
destionationrule:
  enabled: false

gateway:
  enabled: false
  rules:
    - name: test
      additionalSelector: {}
      protocols:
        grpc:
          enabled: false
          port: 10901
        http:
          enabled: false
          port: 80
          httpsRedirect: false
        https:
          enabled: false
          port: 443
          tls:
            mode: SIMPLE
            dnsNames: 
            - mydns.com
            issuerName: "letsencrypt-prod-cluster"

      hosts:
      - mydns.com


virtualservice:
  enabled: false
  rules:
    - name: test
      gatewayName: test
      hosts: 
      - domain.com
      namespace: "default"

      http:
      - match:
          - uri:
              prefix: /
        route:
          - destination:
              host: test
              port:
                number: 8080
      tls: {}
